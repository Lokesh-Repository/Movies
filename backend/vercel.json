{
  "installCommand": "npm install && npx prisma generate",
  "buildCommand": "echo 'Skipping build - using serverless functions'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}